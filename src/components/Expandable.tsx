import {useRef,useState} from 'react';
export function Expandable({children,title}:{children:React.ReactNode;title:string}){const ref=useRef<HTMLDialogElement>(null);const [expanded,setExpanded]=useState(false);return <dialog ref={ref} className={`expandable ${expanded?'expanded':''}`} aria-label={title} onClose={()=>setExpanded(false)}><div className="expand-controls"><strong>{expanded?title:''}</strong><button className="button secondary" onClick={()=>{if(expanded){ref.current?.close();}else{ref.current?.showModal();setExpanded(true);}}}>{expanded?'Close large view':'Large view'}</button></div>{children}</dialog>;}
